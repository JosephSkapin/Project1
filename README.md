# Project1
running the project goes as follows

1. Run the make file just by executing "make" in the terminal inside of the Project1 Folder
2. then to execute the leak counter do "./leakcount ./testprogram"

